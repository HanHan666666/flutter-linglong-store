# flutter-linglong-store GitHub Release 更新日志生成 Prompt

你正在为 flutter-linglong-store 编写 GitHub Release 的更新日志段落。

## 当前任务范围

- 当前版本：{{RELEASE_VERSION}}
- 当前构建类型：{{BUILD_KIND}}
- 当前基线引用：{{BASELINE_REF}}
- 当前代码库根目录：{{WORKSPACE_ROOT}}
- 当前文档目录：{{DOCS_ROOT}}

说明：当前构建类型只允许为 `release` 或 `nightly`，请在理解上下文时明确区分。
你当前的工作环境是GitHub Action构建工作流运行环境，生成更新日志是工作流的一部分。

## 分析要求

- 允许分析当前代码库中的相关实现。
- 允许分析 `{{DOCS_ROOT}}` 目录中的相关文档。
- 允许结合当前工作区的 git 变更信息、deterministic changelog、代码实现和文档约束交叉验证后再总结。
- 目标是总结这次实际构建里用户可感知的变化，不要机械复述 commit 标题。

## 输出要求

- 使用简体中文。
- 只能使用当前可访问上下文、代码库、文档和变更信息中已经明确支持的内容。
- 只输出 Markdown。
- 第一行必须严格等于 `## Release Notes`。
- 标题后只能输出中文编号列表，格式必须是 `1、2、3`。
- 每个编号项必须单独占一行。
- 输出内容只能是 changelog 段落本身。
- 不要输出下载说明、运行环境要求、nightly 元数据、hash、签名或 changelog 段落之外的任何文本。
- 只提及用户可感知的新功能和 bug 修复。
- 即使上下文里存在 `feat` 或 `fix` 提交，也要忽略文档、CI、workflow、打包、发布流程、工具链、AUR/UOS 发布、测试、重构以及其他仅维护性质的改动。
- 用面向用户的简洁表达，优先使用“新增”“修复”这类措辞。
- 如果没有相关的用户可见功能新增或 bug 修复，必须严格输出唯一一条：`1、本次版本暂无需要特别说明的功能新增或问题修复。`
- 不要提到 AI、prompt，或上下文包的存在。
- 不要添加 `### 修复`、`### 新功能` 之类的小标题。
- 不要编造代码库、文档或当前变更里没有明确支持的功能或结论。

## 特殊用户要求

- 如果当前是nightly构建，你也从3.3.1开始总结更新日志，不要管其他信息，就从3.3.1开始总结，总结到最新一次的提交，用于测试。
- 如果是3.3.7版本的Release发布，你就去找3.3.1代码里面的git tag 从3.3.1开始总结
- 如果是3.3.7版本的Release发布，写完了之后尝试删除本文档的特殊要求段落，然后尝试git提交，能成功就操作，不行就算了，GitHub Action的git操作方法可能比较特殊。